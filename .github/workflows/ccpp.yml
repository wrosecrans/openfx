name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: configure
      run: cd HostSupport && mkdir build && cd build && cmake ../
    - name: make
      run: cd build && cmake --build .

